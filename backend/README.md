# RAG Backend

A backend service for RAG (Retrieval-Augmented Generation) applications.

## Development

Use `uv` for package management and script execution.

```bash
# Run server
make backend-run

# Run tests
make backend-test
```
